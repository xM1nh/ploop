import {
  typeDef as sprayTypeDef,
  resolvers as sprayResolvers,
  SpraySource,
} from "./spray";
import {
  typeDef as commentTypeDef,
  resolvers as commentResolvers,
  CommentSource,
} from "./comment";
import {
  typeDef as likeTypeDef,
  resolvers as likeResolvers,
  LikeSource,
} from "./like";
import {
  typeDef as saveTypeDef,
  resolvers as saveResolvers,
  SaveSource,
} from "./save";

export {
  sprayTypeDef,
  sprayResolvers,
  SpraySource,
  commentTypeDef,
  commentResolvers,
  CommentSource,
  likeTypeDef,
  likeResolvers,
  LikeSource,
  saveTypeDef,
  saveResolvers,
  SaveSource,
};
